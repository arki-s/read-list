<div class="calendar-container">
  <%= month_calendar(events: @meetings, end_attribute: :end_date) do |date, meetings| %>
    <%= date %>
    <% meetings.each do |meeting| %>
      <div class="meeting">
        <p><%= meeting.name %></p>
        <small><%= meeting.time unless meeting.multi_days? %></small>
      </div>
    <% end %>
  <% end %>
</div>
